# SystemDbProvisioner runs before Spring beans are created to ensure the system Postgres
# container is up and accepting connections before HikariCP attempts to connect.
org.springframework.context.ApplicationContextInitializer=\
  com.dbdeployer.config.SystemDbProvisioner
